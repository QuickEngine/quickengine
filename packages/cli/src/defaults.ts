export const DEFAULT_API_URL = "https://api.quickdash.xyz";

// Release Please updates package versions, while this value is embedded in generated projects.
// The scaffold test locks it to packages/sdk/package.json so a release cannot drift silently.
export const QUICK_SDK_VERSION = "0.2.1";
