name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    if: false

    steps:
      - name: Placeholder
        run: echo "Enable this workflow after semantic-release is configured."
