# Templates

Reusable scaffolding templates live here.

These templates are intentionally plain text for now. Once the repo is stable, they can be wired into a `scripts/create-app.ts` generator.

Most product expansion should use a module or workspace recipe instead of this app
template. A new deployable application requires a genuine product boundary.
